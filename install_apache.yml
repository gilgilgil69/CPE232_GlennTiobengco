---
- hosts: all
  become: true

  tasks:
    - name: Install Apache and PHP
      package:
        name:
          - "{{ apache_package }}"
          - "{{ php_package }}"
        state: present
        update_cache: yes


